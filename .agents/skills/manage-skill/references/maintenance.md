# Maintenance

1. Apply current repository policy to affected catalog entries, counts, links, examples, and changelog.
2. Refresh `llms.txt` links from current files; keep the linked sources authoritative.
3. Regenerate linked reference bundles from canonical sources and verify isolated workflow copies. Follow current repository checks: frontmatter and validator tests, mode-specific Definition of Done and bounded convergence, links/loading cycles, catalog consistency, and whitespace.
4. Validate this local skill separately from the public frontmatter inventory; documentation validation covers its family structure and links.
5. Exercise success, failure, and authority scenarios using the [evaluation method](../../../../references/skill-evaluations.md). After rename/deletion, check old references and retained capabilities. For mandatory changes, verify catalog coverage and the documented loader contract; distinguish runtime tests from metadata inspection.
6. Report actual results and concrete recovery steps for gaps.
